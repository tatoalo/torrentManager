name: build 👷‍♂️

on:
  pull_request:
  push:
    branches:
      - "**"
      - "!main"
    tags-ignore:
      - "**"

jobs:
  build_test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: docker build .
