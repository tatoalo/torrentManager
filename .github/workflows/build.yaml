name: build 👷‍♂️

on:
  pull_request:
  push:
    branches:
      - "**"
      - "!main"
    tags-ignore:
      - "**"

jobs:
  build_ubuntu:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

  build_docker:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: docker build .
